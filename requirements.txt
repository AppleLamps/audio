# requirements.txt

streamlit>=1.20.0       # Core Streamlit framework
openai>=1.3.0         # OpenAI API client library (ensure v1.0+ for async)
websockets>=11.0      # For WebSocket communication with Realtime API
sounddevice>=0.4.6    # For audio input/output
numpy>=1.20.0         # sounddevice often depends on numpy

# Optional, but good practice:
python-dotenv>=0.19.0 
